server:
  port: 8080

spring:
  application:
    name: exercise

  datasource:
    url: jdbc:h2:file:./data/companyApp;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  sql:
    init:
      mode: always


